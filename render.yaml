services:
  - type: web
    name: rhk-gradio-app
    env: python
    plan: free
    buildCommand: pip install -r requirements.txt
    startCommand: python rhk_app_web_master.py
    envVars:
      - key: PYTHON_VERSION
        value: 3.11.9
